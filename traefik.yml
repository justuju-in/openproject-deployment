entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false

certificatesResolvers:
  myresolver:
    acme:
      email: chhotu@justuju.in        # your real email
      storage: /acme.json
      httpChallenge:
        entryPoint: web                # http-01 challenge on port 80

api:
  dashboard: true

log:
  level: INFO
